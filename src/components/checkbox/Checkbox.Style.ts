import { Box, styled } from '@mui/material'

const CheckBoxStyledContainer = styled(Box)`
  .MuiButtonBase-root {
    padding: 0;
  }
  .MuiTypography-root {
    margin: 0 8px;
  }
` as typeof Box

export default CheckBoxStyledContainer
