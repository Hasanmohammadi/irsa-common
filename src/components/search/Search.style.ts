import { Box } from '@mui/material'
import { styled } from '@mui/material/styles'

const SearchStyledContainer = styled(Box)`
  .clbaxI {
    padding: 0 8px 0 14px;
    margin-top: -3px;
  }
` as typeof Box

export default SearchStyledContainer
